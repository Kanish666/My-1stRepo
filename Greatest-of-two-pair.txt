
#include <stdio.h>
#include<iostream>
using namespace std;

class Solution{
  public:
  int Sum ( int numb){
      
      int temp[4];
      int Ar = 0;
      
      while(numb){
          
          temp[Ar]=numb%10;
          numb = numb/10;
          Ar++;
        };
        
        for(int i = 0; i < 4; i++){
            for(int j = 0; j < 3-i; j++){
                if(temp[j] > temp[j+1]){
                    int tmp = temp[j];
                    temp[j] = temp[j+1];
                    temp[j+1] = tmp;
                };
                 
            };
             
        }
      
       int num1 = temp[0] * 10 + temp[2];
       int num2 = temp[1] * 10 + temp[3];
       
        cout<< num1 + num2;

        return 0;
  };
  
};



int main()
{
   Solution obj;
   int num;
   cout<<"Input Fout Digit Number";
   cin>>num;
   
   //if(num.length>4){
      // cout<<"input smaller number";
  // }
  // else{
       
     obj.Sum(num);
   
  // }
}
